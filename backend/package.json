{
    "name": "voiceforge-backend",
    "version": "1.0.0",
    "description": "Voice Forge — AI Voice-to-Action Automation Engine",
    "main": "server.js",
    "engines": {
        "node": ">=20.0.0"
    },
    "scripts": {
        "start": "node server.js",
        "dev": "node --watch server.js",
        "start:prod": "NODE_ENV=production node server.js",
        "start:pm2": "pm2 start ../ecosystem.config.js --env prod",
        "docker:build": "docker build -t voiceforge-api .",
        "docker:run": "docker run -p 3001:3001 --env-file .env voiceforge-api",
        "health": "curl -s http://localhost:3001/api/health | node -e \"process.stdin.on('data',d=>console.log(JSON.parse(d)))\"",
        "lint": "echo 'Add eslint config for production linting'"
    },
    "dependencies": {
        "@google/generative-ai": "^0.21.0",
        "bcryptjs": "^3.0.3",
        "compression": "^1.8.1",
        "cors": "^2.8.5",
        "dotenv": "^16.4.7",
        "express": "^4.21.2",
        "express-rate-limit": "^7.5.0",
        "helmet": "^8.0.0",
        "jsonwebtoken": "^9.0.3",
        "mongoose": "^9.2.1",
        "pino": "^10.3.1",
        "pino-pretty": "^13.1.3",
        "twilio": "^5.4.3",
        "uuid": "^11.0.5"
    }
}
